using System;
namespace Onnx
{
    public enum AttrType
    {
        FLOAT,
        INT,
        STRING,
        TENSOR,
        GRAPH,
        SPARSE_TENSOR,
        FLOATS,
        INTS,
        STRINGS,
        TENSORS,
        GRAPHS,
        SPARSE_TENSORS
    }
}
